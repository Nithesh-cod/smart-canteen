// Decorative cyber-HUD FX removed in the Warm-Glass redesign — kept as a no-op
// so existing imports keep working. The warm ambient backdrop (index.css
// body::before) now provides the atmosphere instead.
import React from 'react';
export const GridFloor: React.FC<any> = () => null;
export default GridFloor;
